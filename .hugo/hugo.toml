baseURL = 'https://example.org/'
title = 'Computer Notebook'


#hasSummaryDivider = true # default
#summaryLength = 20
disablePathToLower = true # Disable URL lowercasing (preserve original case)
contentDir = "."
layoutDir  = ".hugo/layouts"
assetDir  = ".hugo/assets"
staticDir  = ".hugo/static"
publishDir = ".hugo/public"


ignoreFiles = [
  '/\.hugo/',   # .hugo/ ディレクトリを除外
  'README\.md$', # README等を除外
  '/99-configs/'
]




defaultContentLanguage = 'ja'
[languages]
  [languages.ja]
    locale = 'ja'


[params]
  mainSections = ["test"]
  dateFormat = "2006-01-02T15:04:05Z07:00"

  # Regular expressions (RE2 syntax) matching tag names to hide from the tag list page
  excludedTagPatterns = ["^INQ-", "^EXP-"]

[markup]
  [markup.highlight]
    codeFences  = true   # Enable fenced code blocks (``` style)
    noClasses   = true   # Use inline styles (no external CSS needed)
    style       = "monokai"
    lineNos     = false  # Enable if you want line numbers
    guessSyntax = false  # Auto-detection is unreliable; always specify language

  [markup.tableOfContents]
    startLevel = 2
    endLevel = 6
    ordered = false
  [markup.goldmark.renderer]
    unsafe = true   # Allow raw HTML in markdown content
